# =============================================================================
# Gordon IT Academy — Frontend (TEMPLATE)
# =============================================================================
# Copy to `.env` for local dev:
#   cp .env.example .env
#
# For production, do NOT put live URLs in a committed .env file — set them
# as build-time environment variables in your CI/CD pipeline instead
# (see .github/workflows/deploy.yml). Example production values:
#   VITE_API_BASE=https://api.yourdomain.com/api
#   VITE_DASHBOARD_URL=https://admin.yourdomain.com
# =============================================================================

VITE_API_BASE=http://localhost:8000/api
VITE_DASHBOARD_URL=http://localhost:3001
